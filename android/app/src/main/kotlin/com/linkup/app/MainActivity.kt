package com.linkup.app

import io.flutter.embedding.android.FlutterActivity

/**
 * MainActivity is intentionally minimal.
 *
 * All WebView logic lives in lib/webview_screen.dart.
 * FlutterActivity handles the embedding lifecycle, back-press routing,
 * and plugin registration automatically via GeneratedPluginRegistrant.
 */
class MainActivity : FlutterActivity()
